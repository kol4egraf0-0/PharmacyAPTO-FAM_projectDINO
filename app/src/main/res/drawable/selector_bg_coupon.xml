<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Состояние при нажатии -->
    <item android:state_pressed="true" android:drawable="@drawable/bg_coupon_pressed" />
    <!-- Состояние по умолчанию -->
    <item android:drawable="@drawable/bg_coupon_default" />
</selector>